# Generated Training Corpus — Status Report

**Date:** 2026-05-07
**Commit:** TBD (after changes are committed)

---

## Source Discovery

### Repo source files found

None. The following locations were checked:

- `data/source-games.pgn`
- `data/source-games.json`
- `data/source-games.ndjson`
- `data/source-games.jsonl`
- `public/source-games.pgn`
- `public/source-games.json`
- `data/games.pgn`
- `data/games.json`
- `data/games.ndjson`
- `data/lichess-games.pgn`
- `data/lichess-games.json`
- `data/lichess-games.ndjson`

### Verdict

**No authoritative source data exists in the repository.** Generation was not run.

---

## Existing Datasets (all invalid)

| File | Rows | Valid Preludes |
|------|------|----------------|
| `public/training_tactic_positions.json` | 25 | 0 |
| `public/training_opening_positions.json` | 30 | 0 |
| `public/training_endgame_positions.json` | 21 | 0 |
| `public/elite_positions.json` | 1000 | 0 |

All rows fail because they lack `previousFen`. They were fabricated as seed/filler data.

---

## What the Generator Supports

### Accepted source formats

| Format | Extension | Description |
|--------|-----------|-------------|
| PGN | `.pgn` | Multi-game PGN with full move text in SAN. chess.js `loadPgn()` is used to parse. |
| JSON | `.json` | JSON array of game objects with a `moves` field (space-separated UCI string). |
| NDJSON | `.ndjson` / `.jsonl` | One JSON game object per line, same schema as JSON. |

### Rejected source types

- FEN-only datasets (no legal move history)
- Puzzle FEN + solution with no previous move
- Manually guessed previous positions
- Positions generated by reversing moves from FEN
- Random legal self-play games

### Game object schema (JSON/NDJSON)

```json
{
  "moves": "e2e4 e7e5 g1f3 b8c6 f1b5",
  "id": "game_123",
  "tags": ["blitz", "2200+"]
}
```

The `moves` field is required. `id` and `tags` are optional.

### CLI usage

```
node scripts/generate-training-corpus.mjs --source <path> [options]

Options:
  --out <dir>         Output directory (default: public/generated-training)
  --opening <n>       Target opening positions (default: 10000)
  --middlegame <n>    Target middlegame positions (default: 10000)
  --endgame <n>       Target endgame positions (default: 10000)
  --puzzles <n>       Target puzzle positions (default: 0)
  --seed <n>          Random seed (default: 42)
  --strict            Exit non-zero on shortfalls
  --dry-run           Validate and report without writing files
```

### Pipeline steps

1. Load source file (PGN, JSON, or NDJSON)
2. Replay each game with chess.js, capturing `previousFen` before every move
3. Validate every position via inlined `validateSetupPrelude` (mirrors `lib/training/setup-prelude.ts`)
4. Deduplicate by normalized FEN key (board + side + castling + en passant)
5. Exclude terminal positions (checkmate, stalemate, draw, no legal moves)
6. Classify by phase (opening ply ≤20, middlegame ply 21-70, endgame based on material)
7. Shuffle with seeded PRNG, sample to target counts
8. Write output JSON files

### Phase heuristics

- **Opening:** ply between 1 and 20 inclusive
- **Middlegame:** ply between 21 and 70, not classified as endgame
- **Endgame:** ≤10 pieces, OR no queens + low non-pawn material (≤6 pieces), OR ply >70 with ≤14 pieces

---

## Generated Outputs

No files generated. Source data required.

Output files that would be written:

- `public/generated-training/generated_training_opening_positions.json`
- `public/generated-training/generated_training_middlegame_positions.json`
- `public/generated-training/generated_training_endgame_positions.json`

Puzzle corpus is skipped (no authoritative puzzle source with move history).

---

## Puzzle Corpus Status

**Skipped.** The generator has no puzzle-specific source identification logic. Puzzle rows are only generated if the source contains puzzles with full move history. No such source exists in the repo.

---

## Validation Integration

The validation script (`scripts/validate-training-preludes.mjs`) now supports:

- `--include-generated` flag to explicitly include generated dataset files
- Auto-inclusion of generated files if they exist in `public/generated-training/`

Run with:
```
npm run validate:training-preludes -- --include-generated
```

---

## Tests

All 41 tests pass:

- `tests/stockfish-uci-parser.test.ts` — 5/5 passed
- `tests/stockfish-client-lines-to-training-lines.test.ts` — 9/9 passed
- `tests/training-setup-prelude.test.ts` — 5/5 passed
- `tests/training-corpus-helpers.test.ts` — 22/22 passed

---

## Build

Build has a pre-existing TypeScript error in `app/(shell)/train/train-client.tsx:1898` (unrelated to this work).

---

## Blockers

| Blocker | Status |
|---------|--------|
| No source game file in repo | **Blocking** |
| No puzzle source in repo | **Blocking** (expected) |
| Pre-existing TypeScript error | Pre-existing, unrelated |

---

## Exact Next Step

1. Obtain an authoritative game source file (PGN or JSON with move history)
2. Place it at `data/source-games.pgn` (or any path)
3. Run:
   ```
   npm run generate:training-corpus -- --source data/source-games.pgn --opening 10000 --middlegame 10000 --endgame 10000 --puzzles 10000
   ```
4. Run validation:
   ```
   npm run validate:training-preludes -- --include-generated
   ```
5. If validation passes, the generated JSON files can be committed and the serving layer can be switched to use them.

### Recommended source data

- **Lichess Elite/Standard PGN dumps** — https://database.lichess.org/ (monthly PGN files with full move history)
- **Lichess puzzle database** — https://database.lichess.org/#puzzles (contains game_id for reconstructing move history)
- **JSON/NDJSON game exports** with UCI or SAN move lists
